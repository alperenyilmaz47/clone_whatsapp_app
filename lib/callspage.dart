import 'package:flutter/material.dart';

class callspage extends StatelessWidget {
  const callspage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("testcalls"),
      ),
    );
  }
}
